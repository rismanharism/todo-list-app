<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Edit Task</h1>

    <form action="/update/<%= task.id %>" method="POST">
        <input type="text" name="title" value="<%= task.title %>" required />
        <textarea name="description"><%= task.description %></textarea>
        <input type="checkbox" name="status" <%= task.status ? 'checked' : '' %> /> Completed
        <button type="submit">Update Task</button>
    </form>

    <div class="task-dates">
        <span><strong>Created:</strong> <%= new Date(task.createdAt).toLocaleString() %></span><br>
        <span><strong>Last Updated:</strong> <%= new Date(task.updatedAt).toLocaleString() %></span>
    </div>
</body>
</html>
